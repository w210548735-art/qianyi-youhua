[project]
name = "qianyi-youhua"
version = "0.1.0"
description = "黔衣有话：贵州文旅博主 AI 内容资产与运营辅助产品"
requires-python = ">=3.10,<3.13"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "sqlalchemy>=2.0",
  "alembic>=1.19",
  "pydantic>=2.0",
  "httpx>=0.27",
  "jinja2>=3.1",
  "numpy>=1.26",
  "sentence-transformers>=3.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "pytest-cov>=7.0",
  "ruff>=0.8",
  "mypy>=1.15",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B"]

[tool.ruff.lint.per-file-ignores]
"app/api/*.py" = ["B008"]

[tool.mypy]
python_version = "3.10"
plugins = ["pydantic.mypy"]
check_untyped_defs = true
ignore_missing_imports = true
exclude = ["models", "data"]
